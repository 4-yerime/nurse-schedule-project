<UserControl x:Class="NurseScheduler.UI.Views.Nurses.NurseListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:viewModels="clr-namespace:NurseScheduler.UI.ViewModels"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="500">

    <UserControl.DataContext>
        <viewModels:NurseListViewModel />
    </UserControl.DataContext>

    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding Nurses}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Ad" Binding="{Binding FirstName}" Width="*"/>
                <DataGridTextColumn Header="Soyad" Binding="{Binding LastName}" Width="*"/>
                <DataGridCheckBoxColumn Header="Baş Hemşire" Binding="{Binding IsHeadNurse}" Width="100"/>
                <DataGridTextColumn Header="Alt Birim ID" Binding="{Binding SubUnitId}" Width="100"/>
                <DataGridTextColumn Header="Çalışma Tipi" Binding="{Binding EmploymentType}" Width="100"/>
                <DataGridTextColumn Header="Max Aylık Saat" Binding="{Binding MaxMonthlyHours}" Width="120"/>
                <DataGridTextColumn Header="Yıllık İzin" Binding="{Binding AnnualLeaveBalance}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>