<Application x:Class="NurseScheduler.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:helpers="clr-namespace:NurseScheduler.UI.Helpers"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="Indigo" SecondaryColor="Cyan"/>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <helpers:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
            <helpers:InverseBoolConverter x:Key="InverseBoolConverter"/>
            <helpers:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyConverter"/>
            <helpers:EqualityConverter x:Key="EqualityConverter"/>

            <!-- App Brushes -->
            <SolidColorBrush x:Key="AppBackground" Color="#0F0F1A"/>
            <SolidColorBrush x:Key="NavBackground" Color="#13131F"/>
            <SolidColorBrush x:Key="CardBackground" Color="#1A1A2E"/>
            <SolidColorBrush x:Key="AccentBlue" Color="#3D5AFE"/>
            <SolidColorBrush x:Key="AccentCyan" Color="#00BCD4"/>
            <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#B0BEC5"/>
            <SolidColorBrush x:Key="HardRuleBrush" Color="#EF5350"/>
            <SolidColorBrush x:Key="SoftRuleBrush" Color="#FFB300"/>
            <SolidColorBrush x:Key="SuccessGreen" Color="#00C853"/>

            <!-- Nav Button -->
            <Style x:Key="NavButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                <Setter Property="Foreground" Value="#7B8BB2"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Padding" Value="12,10"/>
                <Setter Property="Height" Value="42"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Margin" Value="6,1"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="#1E1E3A"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Page Title -->
            <Style x:Key="PageTitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="22"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Margin" Value="0,0,0,20"/>
            </Style>

            <!-- Stat Value -->
            <Style x:Key="StatValueStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="34"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="#00BCD4"/>
            </Style>

            <!-- Stat Label -->
            <Style x:Key="StatLabelStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="#78909C"/>
                <Setter Property="Margin" Value="0,6,0,0"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
